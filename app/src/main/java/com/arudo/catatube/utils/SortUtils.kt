package com.arudo.catatube.utils

object SortUtils {
    const val newest = "Newest"
    const val oldest = "Oldest"

//    fun getSortedQuery(filter: String): SimpleSQLiteQuery{
//        val movieQuery = StringBuilder().append("SELECT * From movieentity");
//    }
}